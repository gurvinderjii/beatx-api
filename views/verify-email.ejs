<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Email Verification - BeatX</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap');
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #667eea, #764ba2);
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #333;
        }
        .container {
            background: white;
            padding: 3rem 2rem;
            border-radius: 15px;
            box-shadow: 0 8px 24px rgba(0,0,0,0.15);
            width: 90%;
            max-width: 450px;
            text-align: center;
            opacity: 0;
            animation: fadeIn 0.7s ease forwards;
        }
        @keyframes fadeIn {
            to { opacity: 1; }
        }
        .icon {
            margin-bottom: 1.5rem;
            font-size: 72px;
            color: #4caf50;
            animation: bounceIn 0.6s ease forwards;
        }
        .icon.error {
            color: #e74c3c;
        }
        @keyframes bounceIn {
            0% {
                transform: scale(0);
            }
            60% {
                transform: scale(1.2);
            }
            100% {
                transform: scale(1);
            }
        }
        h1 {
            margin: 0 0 1rem;
            font-weight: 600;
            font-size: 1.8rem;
            color: #333;
        }
        p {
            font-size: 1rem;
            color: #555;
            margin-bottom: 2rem;
            line-height: 1.5;
        }
        .btn {
            display: inline-block;
            padding: 0.75rem 2rem;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            color: white;
            background: #667eea;
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.5);
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
        }
        .btn:hover {
            background: #5563c1;
            box-shadow: 0 6px 20px rgba(85, 99, 193, 0.7);
        }
    </style>
</head>
<body>
<div class="container">
    <% if (success) { %>
        <div class="icon">✅</div>
        <h1>Email Verified Successfully!</h1>
        <p><%= message || "Thanks for verifying your email. You can now access BeatX." %></p>
        <a href="/login" class="btn">Go to Login</a>
    <% } else { %>
        <div class="icon error">❌</div>
        <h1>Verification Failed</h1>
        <p><%= message || "The verification link is invalid or expired." %></p>
        <a href="/resend-verification" class="btn">Resend Verification Email</a>
    <% } %>
</div>
</body>
</html>